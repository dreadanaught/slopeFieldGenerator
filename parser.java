import java.util.ArrayList;

public class parser {

	//Converts strings from standard algebraic notation to Reverse Polish Notation
	//Really needs to use stacks for this instead of ArrayLists, but I can't be bothered to learn how to do that right now.
	public static ArrayList<String> shunt(String input) {
		ArrayList<String> output = new ArrayList<>();
		ArrayList<String> operatorStack = new ArrayList<>();

		for (int i = 0; i < input.length(); i++) {
			
		}
		
		
		
		return output;
	}
}